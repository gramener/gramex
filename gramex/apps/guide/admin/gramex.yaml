import:
  admin:
    path: $GRAMEXAPPS/admin/gramex.yaml   # Source of the app
    YAMLURL: $YAMLURL/admin/              # Location to mount at
    ADMIN_LOOKUP:
      url: $YAMLPATH/../auth/lookup.xlsx
      id: user
    ADMIN_USER: ['alpha']
    ADMIN_ROLE: ['admin']
    LOGIN_URL: /$YAMLURL/../auth/db
    LOGOUT_URL: /$YAMLURL/../auth/logout
