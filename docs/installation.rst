============
Installation
============

At the command line::

    $ easy_install gramex

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv gramex
    $ pip install gramex
